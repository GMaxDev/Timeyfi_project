import Bloc from "@/app/components/bloc";

export default function Dashboard() {
  return (
    <div className="flex gap-2 flex-wrap relative relative">
      <Bloc posUnitX={0} posUnitY={1} blocSizeX={1} blocSizeY={1}/>
    </div>
  );
}
